# clean_arch_init Brick

Templates used by `clean_arch_cli init` to scaffold core clean-architecture files and sample TBGs.

Variables:
- appName: The Dart package name of the target project (used for import paths).
